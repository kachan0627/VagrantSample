sync:
    app:
      mode: "two-way-resolved"
      alpha: "./"
      beta: "sample-app:/home/vagrant/app"
      ignore:
        vcs: true
        paths:
          - "/node_modules"
          - "/log"
          - "/tmp"